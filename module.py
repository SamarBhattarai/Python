# module is a code library written by another programmer that has functions which we can use
# if a certain module is not installed we need to install it using pip install tensorflow / pip3 install tensorflow
import os
os.remove("sample.txt")