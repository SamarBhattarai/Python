# Unlike lists they are immutable (like strings)
tup = (2,1,3,4)
tup1 = () #empty tuple
tup2 = (1) # it is seen as integer
tup3 = (1,) # now only it is seen as tuple comma is necessary
tup4 = (1,"samar")
print(tup4)
print(tup4[1])

# tuple slicing is same as list slicing